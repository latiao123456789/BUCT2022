<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication" >
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
             android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication" >
        <activity android:name=".ui.login.SignupAct"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.home.HomeAct" />
        <activity android:name=".ui.home.search.search" />
        <activity android:name=".ui.wenwuMainPage.comment.commentMainPage" />
        <activity android:name=".ui.wenwuMainPage.wenwuMainPage" />
        <activity android:name=".ui.userMainPage.userMainPageAct" />
        <activity android:name=".ui.userMainPage.changeInfoAct" />
        <activity android:name=".ui.login.LoginAct" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
</manifest>